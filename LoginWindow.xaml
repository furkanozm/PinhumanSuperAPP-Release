<Window x:Class="WebScraper.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pinhuman SuperApp - Giriş"
        Height="760" Width="470"
        WindowStartupLocation="CenterScreen"
        MinHeight="650" MinWidth="450"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="pack://application:,,,/Fonts/Poppins-Regular.ttf#Poppins">
    
    <Window.Resources>
        <!-- Modern UI Styles -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentPresenter" 
                                            HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#CCCCCC"/>
                                <Setter TargetName="ContentPresenter" Property="Opacity" Value="0.5"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1976D2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="10,8"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="10,8"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border Background="White" 
            CornerRadius="15" 
            BorderBrush="#E0E0E0" 
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#333333" 
                            Direction="270" 
                            ShadowDepth="15" 
                            BlurRadius="25" 
                            Opacity="0.2"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header - Kapatma Butonu -->
            <Border Grid.Row="0"
                    Background="#0d47a1"
                    CornerRadius="15,15,0,0"
                    Height="60"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>

                    <!-- Sol taraf - Boş alan -->
                    <Grid Grid.Column="0"/>

                    <!-- Orta - Başlık -->
                    <TextBlock Grid.Column="1"
                               Text="Pinhuman SuperApp"
                               FontSize="18"
                               FontFamily="pack://application:,,,/Fonts/Poppins-Bold.ttf#Poppins"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>

                    <!-- Sağ taraf - Butonlar -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <!-- Tam Ekran Butonu -->
                        <Button x:Name="btnFullscreen"
                                Content="⛶"
                                FontSize="16"
                                Foreground="White"
                                Background="Transparent"
                                BorderThickness="0"
                                Width="40"
                                Height="40"
                                VerticalAlignment="Center"
                                Click="btnFullscreen_Click"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent"
                                            CornerRadius="0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#42a5f5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Kapatma Butonu -->
                        <Button x:Name="btnClose"
                                Content="✕"
                                FontSize="16"
                                Foreground="White"
                                Background="Transparent"
                                BorderThickness="0"
                                Width="40"
                                Height="40"
                                VerticalAlignment="Center"
                                Click="btnClose_Click"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent"
                                            CornerRadius="0,15,0,0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#d32f2f"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Ana İçerik -->
            <Grid Grid.Row="1" Margin="40,30">
                <StackPanel>
                    <!-- Başlık -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                        <Border x:Name="lockIconBorder"
                                Cursor="Hand"
                                Padding="5"
                                Margin="0,0,8,0"
                                MouseLeftButtonDown="LockIcon_MouseLeftButtonDown"
                                MouseEnter="LockIcon_MouseEnter"
                                MouseLeave="LockIcon_MouseLeave"
                                ToolTip="Güncelleme Notlarını Görüntüle">
                            <TextBlock x:Name="lockIconText"
                                       Text="🔐"
                                       FontSize="28"
                                       FontFamily="pack://application:,,,/Fonts/Poppins-Bold.ttf#Poppins"
                                       FontWeight="Bold"
                                       Foreground="#0d47a1"/>
                        </Border>
                        <TextBlock Text="Giriş Yap"
                                   FontSize="28"
                                   FontFamily="pack://application:,,,/Fonts/Poppins-Bold.ttf#Poppins"
                                   FontWeight="Bold"
                                   Foreground="#0d47a1"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- Logo Container -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
                        <!-- Güleryüz Logo -->
                        <Border Background="#0d47a1" 
                                CornerRadius="8" 
                                Height="50"
                                Width="140"
                                Margin="0,0,10,0"
                                Cursor="Hand"
                                MouseLeftButtonDown="LogoBorder_MouseLeftButtonDown">
                            <Image Source="pack://application:,,,/güleryüz logo (1)2.png" 
                                   Width="120" Height="120" 
                                   Stretch="Uniform" 
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                        </Border>
                        
                        <!-- Pinhuman Logo -->
                        <Border Background="#0d47a1" 
                                CornerRadius="8" 
                                Height="50"
                                Width="140"
                                Cursor="Hand"
                                MouseLeftButtonDown="PinhumanLogo_MouseLeftButtonDown">
                            <Image Source="pack://application:,,,/logoya.png" 
                                   Width="120" Height="120" 
                                   Stretch="Uniform" 
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                    
                    <!-- Giriş Mesajı -->
                    <TextBlock x:Name="txtLoginMessage" 
                               Text="Sisteme giriş yapmak için email ve şifrenizi girin:" 
                               FontSize="14" 
                               Foreground="#333"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               Margin="0,0,0,20"/>
                    
                    <!-- Email Label -->
                    <TextBlock Text="📧 Email:" 
                               FontSize="14" 
                               Foreground="#333"
                               HorizontalAlignment="Left"
                               Margin="0,0,0,5"/>
                    
                    <!-- Email Girişi -->
                    <TextBox x:Name="txtLoginEmail" 
                             Style="{StaticResource ModernTextBox}" 
                             FontSize="14"
                             Height="45"
                             Margin="0,0,0,15"
                             KeyDown="txtLoginEmail_KeyDown"
                             TextChanged="txtLoginEmail_TextChanged"/>
                    
                    <!-- Şifre Label -->
                    <TextBlock Text="🔒 Şifre:" 
                               FontSize="14" 
                               Foreground="#333"
                               HorizontalAlignment="Left"
                               Margin="0,0,0,5"/>
                    
                    <!-- Şifre Girişi -->
                    <PasswordBox x:Name="txtLoginPassword" 
                                 Style="{StaticResource ModernPasswordBox}" 
                                 FontSize="16"
                                 Height="45"
                                 Margin="0,0,0,15"
                                 PasswordChar="●"
                                 KeyDown="txtLoginPassword_KeyDown"
                                 GotFocus="txtLoginPassword_GotFocus"
                                 LostFocus="txtLoginPassword_LostFocus"/>
                    
                    <!-- Caps Lock Uyarısı -->
                    <Border x:Name="capsLockWarning" 
                            Background="#FFF3CD" 
                            BorderBrush="#FFEAA7" 
                            BorderThickness="1" 
                            CornerRadius="8" 
                            Padding="10,8" 
                            Margin="0,0,0,15"
                            Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚠️" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Caps Lock açık!" 
                                       FontSize="12" 
                                       Foreground="#856404" 
                                       FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Beni Hatırla ve Şifremi Unuttum Satırı -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Beni Hatırla Checkbox -->
                        <CheckBox x:Name="chkRememberMe" 
                                  Content="Beni Hatırla" 
                                  FontSize="12" 
                                  Foreground="#666"
                                  HorizontalAlignment="Left"
                                  VerticalAlignment="Center"/>
                        
                        <!-- Şifremi Unuttum Butonu -->
                        <Button x:Name="btnForgotPassword"
                                Content="Şifremi Unuttum"
                                FontSize="12"
                                Foreground="#2196F3"
                                Background="Transparent"
                                BorderThickness="0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Cursor="Hand"
                                Click="btnForgotPassword_Click"
                                Grid.Column="1">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 17h-2v-6h2v6zm0-8h-2V7h2v4z"
                                              Fill="{TemplateBinding Foreground}"
                                              Width="22"
                                              Height="22"
                                              Margin="0,0,3,0"/>
                                        <TextBlock Text="{TemplateBinding Content}"
                                                   Foreground="{TemplateBinding Foreground}"
                                                   FontSize="{TemplateBinding FontSize}"/>
                                    </StackPanel>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#1976D2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                    
                    <!-- Giriş Butonu -->
                    <Button x:Name="btnLogin"
                            Style="{StaticResource ModernButton}"
                            FontFamily="pack://application:,,,/Fonts/Poppins-Regular.ttf#Poppins"
                            FontSize="14"
                            FontWeight="Normal"
                            Height="50"
                            Background="#0d47a1"
                            Margin="0,0,0,15"
                            Click="btnLogin_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔐" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Giriş Yap" FontSize="14"/>
                        </StackPanel>
                    </Button>
                    
                    <!-- Kullanıcı Talebi Butonu -->
                    <Button x:Name="btnUserRequest"
                            Content="👤 Kullanıcı Talebinde Bulun"
                            FontFamily="pack://application:,,,/Fonts/Poppins-Regular.ttf#Poppins"
                            FontSize="13"
                            FontWeight="Normal"
                            Foreground="White"
                            Background="#FF9800"
                            BorderThickness="0"
                            Height="45"
                            Margin="0,0,0,15"
                            Click="btnUserRequest_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F57C00"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    
                    <!-- Hata Mesajı -->
                    <TextBlock x:Name="txtLoginError" 
                               Text="" 
                               FontSize="12" 
                               Foreground="#f44336"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap"
                               Margin="0,10,0,0"
                               Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
            
            <!-- Footer -->
            <Border Grid.Row="2"
                    Background="#f5f5f5"
                    CornerRadius="0,0,15,15"
                    Height="40">
                <StackPanel Orientation="Horizontal"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <TextBlock Text="© 2025 Güleryüz Group - Pinhuman SuperApp"
                               FontFamily="pack://application:,,,/Fonts/Poppins-Regular.ttf#Poppins"
                               FontSize="11"
                               Foreground="#666"
                               VerticalAlignment="Center"/>
                    <!-- Versiyon Badge -->
                    <Border x:Name="versionBadge"
                            Background="#2196F3"
                            CornerRadius="12"
                            Padding="8,4"
                            Margin="8,0,0,0"
                            VerticalAlignment="Center">
                        <TextBlock x:Name="txtVersion"
                                   Text=""
                                   FontFamily="pack://application:,,,/Fonts/Poppins-Regular.ttf#Poppins"
                                   FontSize="10"
                                   FontWeight="Normal"
                                   Foreground="White"
                                   VerticalAlignment="Center"/>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
