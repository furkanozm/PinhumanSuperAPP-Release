<Window x:Class="WebScraper.StoredPDKSRecordsModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kayıtlı PDKS Verileri Ekle"
        Height="700"
        Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#F5F5F5"
        TextElement.FontFamily="pack://application:,,,/Fonts/Poppins-Regular.ttf#Poppins">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <TextBlock Grid.Row="0"
                   Text="Kayıtlı PDKS Verileri Ekle"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="#1E3A8A"
                   Margin="0,0,0,20"/>

        <!-- Ana İçerik -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Bilgilendirme -->
                <Border Background="#EEF2FF"
                        CornerRadius="12"
                        Padding="16"
                        Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="📋 Nasıl Kullanılır?"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Foreground="#1E3A8A"
                                   Margin="0,0,0,8"/>
                        <TextBlock Text="Excel dosyasından günlük PDKS kayıtlarını içe aktarın. Her satır bir personelin bir gününü temsil eder."
                                   FontSize="13"
                                   Foreground="#1E3A8A"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,8"/>
                        <TextBlock Text="Excel şablonu formatı:"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="#1E3A8A"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,4"/>
                        <TextBlock Text="Sicil | Ad Soyad | Tarih | Giriş Saati | Çıkış Saati | Vardiya"
                                   FontSize="12"
                                   Foreground="#4B5563"
                                   TextWrapping="Wrap"
                                   FontStyle="Italic"
                                   Margin="0,0,0,4"/>
                        <TextBlock Text="Örnek: 12345 | Ahmet Yılmaz | 2024-10-15 | 08:00 | 17:00 | 8/17"
                                   FontSize="11"
                                   Foreground="#6B7280"
                                   TextWrapping="Wrap"
                                   FontStyle="Italic"/>
                    </StackPanel>
                </Border>

                <!-- Excel Butonları -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <Button x:Name="btnDownloadTemplate"
                            Content="📥 Excel Şablonu İndir"
                            Background="#3B82F6"
                            Foreground="White"
                            Click="btnDownloadTemplate_Click"
                            Margin="0,0,10,0"
                            HorizontalAlignment="Left"/>
                    <Button x:Name="btnImportExcel"
                            Content="📊 Excel'den İçe Aktar"
                            Background="#059669"
                            Foreground="White"
                            Click="btnImportExcel_Click"
                            HorizontalAlignment="Left"/>
                </StackPanel>

                <!-- Dönem Seçimi -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0"
                               Text="Yıl:"
                               FontSize="14"
                               VerticalAlignment="Center"
                               Margin="0,0,10,10"/>
                    <ComboBox x:Name="cmbYear"
                             Grid.Row="0" Grid.Column="1"
                             Height="35"
                             FontSize="14"
                             Margin="0,0,20,10"/>

                    <TextBlock Grid.Row="0" Grid.Column="2"
                               Text="Ay:"
                               FontSize="14"
                               VerticalAlignment="Center"
                               Margin="0,0,10,10"/>
                    <ComboBox x:Name="cmbMonth"
                             Grid.Row="0" Grid.Column="3"
                             Height="35"
                             FontSize="14"
                             Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="1" Grid.Column="0"
                               Text="Firma:"
                               FontSize="14"
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"/>
                    <ComboBox x:Name="cmbCompanyCode"
                             Grid.Row="1" Grid.Column="1"
                             Grid.ColumnSpan="3"
                             Height="35"
                             FontSize="14"
                             DisplayMemberPath="DisplayName"
                             SelectedValuePath="CompanyCode"
                             Margin="0,0,0,0"/>
                </Grid>


                <!-- Hata Mesajı -->
                <Border x:Name="errorBorder"
                        Background="#FEF2F2"
                        BorderBrush="#FCA5A5"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12"
                        Margin="0,0,0,10"
                        Visibility="Collapsed">
                    <TextBlock x:Name="txtErrorMessage"
                               Foreground="#991B1B"
                               FontSize="13"
                               TextWrapping="Wrap"/>
                </Border>

                <!-- Başarı Mesajı -->
                <Border x:Name="successBorder"
                        Background="#F0FDF4"
                        BorderBrush="#86EFAC"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12"
                        Margin="0,0,0,10"
                        Visibility="Collapsed">
                    <TextBlock x:Name="txtSuccessMessage"
                               Foreground="#166534"
                               FontSize="13"
                               TextWrapping="Wrap"/>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Butonlar -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button x:Name="btnCancel"
                    Background="#757575"
                    Foreground="White"
                    Click="btnCancel_Click"
                    Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="✕" FontSize="14" Foreground="White" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="İptal" FontSize="14" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Button x:Name="btnSave"
                    Background="#2196F3"
                    Foreground="White"
                    Click="btnSave_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="✓" FontSize="14" Foreground="White" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Kaydet" FontSize="14" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>

