<Window x:Class="WebScraper.UpdateLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Güncelleme Kontrolü" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        FontFamily="pack://application:,,,/Fonts/Poppins-Regular.ttf#Poppins"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style x:Key="ModalButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentPresenter" 
                                            HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1976D2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#0d47a1" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <TextBlock Text="🔄 Güncelleme Kontrolü"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="White"
                           VerticalAlignment="Center"
                           FontFamily="pack://application:,,,/Fonts/Poppins-Bold.ttf#Poppins"/>
                <Button x:Name="btnClose"
                        Content="✕"
                        FontSize="16"
                        Foreground="White"
                        Background="Transparent"
                        BorderThickness="0"
                        Width="35"
                        Height="35"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Click="btnClose_Click"
                        Cursor="Hand"
                        ToolTip="Kapat">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="Transparent"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center"
                                                VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1976D2"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
        
        <!-- Log Content -->
        <Border Grid.Row="1"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                Background="#FFFFFF"
                CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <ScrollViewer x:Name="logScrollViewer"
                              Grid.Row="0"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto"
                              Padding="15">
                    <TextBlock x:Name="logText"
                               Text=""
                               FontSize="11"
                               Foreground="#333"
                               FontFamily="Consolas"
                               TextWrapping="Wrap"/>
                </ScrollViewer>
                
                <!-- Status Text and Progress Bar -->
                <Border Grid.Row="1"
                        BorderBrush="#E0E0E0"
                        BorderThickness="0,1,0,0"
                        Background="#F9F9F9"
                        Padding="15,10">
                    <StackPanel>
                        <TextBlock x:Name="statusText"
                                   Text="Güncelleme kontrol ediliyor..."
                                   FontSize="12"
                                   Foreground="#666"
                                   FontWeight="SemiBold"
                                   FontFamily="pack://application:,,,/Fonts/Poppins-Regular.ttf#Poppins"
                                   Margin="0,0,0,8"/>
                        
                        <!-- Progress Bar -->
                        <Grid x:Name="progressBarContainer" 
                              Visibility="Collapsed"
                              Margin="0,5,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Progress Text -->
                            <TextBlock x:Name="progressText"
                                       Grid.Row="0"
                                       Text="0%"
                                       FontSize="11"
                                       Foreground="#4CAF50"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Right"
                                       Margin="0,0,0,4"
                                       FontFamily="pack://application:,,,/Fonts/Poppins-Regular.ttf#Poppins"/>
                            
                            <!-- Progress Bar Background -->
                            <Border Grid.Row="1"
                                    Background="#E0E0E0"
                                    CornerRadius="4"
                                    Height="8">
                                <!-- Progress Bar Fill -->
                                <Border x:Name="progressBarFill"
                                        Background="#4CAF50"
                                        CornerRadius="4"
                                        HorizontalAlignment="Left"
                                        Width="0"
                                        Height="8">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#4CAF50"
                                                          BlurRadius="4"
                                                          ShadowDepth="0"
                                                          Opacity="0.3"/>
                                    </Border.Effect>
                                </Border>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button x:Name="btnCopyLogs"
                    Content="📋 Logları Kopyala"
                    Style="{StaticResource ModalButton}"
                    Background="#4CAF50"
                    Margin="0,0,10,0"
                    Width="160"
                    Click="btnCopyLogs_Click"
                    ToolTip="Tüm logları panoya kopyala">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentPresenter" 
                                            HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#45A049"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="btnCloseBottom"
                    Content="Kapat"
                    Style="{StaticResource ModalButton}"
                    Width="120"
                    Click="btnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>

