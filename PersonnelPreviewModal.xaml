<Window x:Class="WebScraper.PersonnelPreviewModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WebScraper"
        mc:Ignorable="d"
        Title="Personel Listesi Önizleme" Height="860" Width="1320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanMinimize"
        Background="#F8F9FA"
        FontFamily="pack://application:,,,/Fonts/Poppins-Regular.ttf#Poppins">

    <Window.Resources>
        <Style x:Key="ModalButton" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45A049"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <Border Grid.Row="0"
                Background="#FFF9C4"
                CornerRadius="8"
                Padding="20"
                Margin="20,20,20,15"
                BorderBrush="#FFEB3B"
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="👥 Personel Listesi Önizleme"
                           FontSize="18"
                           FontFamily="pack://application:,,,/Fonts/Poppins-SemiBold.ttf#Poppins"
                           Foreground="#F57F17"
                           Margin="0,0,0,10"/>
                <TextBlock Text="Şablondan yüklenen personel kayıtlarını kontrol edin. Bilgilerin doğru olduğundan emin olun."
                           FontSize="14"
                           FontFamily="pack://application:,,,/Fonts/Poppins-Regular.ttf#Poppins"
                           Foreground="#424242"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Personel Listesi -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="8"
                Margin="20,0,20,20"
                BorderBrush="#E9ECEF"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Başlık -->
                <TextBlock Grid.Row="0"
                           Text="📋 Personel Kayıtları"
                           FontSize="16"
                           FontFamily="pack://application:,,,/Fonts/Poppins-SemiBold.ttf#Poppins"
                           Foreground="#495057"
                           Margin="20,15,20,15"/>

                <!-- DataGrid -->
                <DataGrid x:Name="dgPersonnelList"
                          Grid.Row="1"
                          Margin="20,0,20,20"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserSortColumns="True"
                          IsReadOnly="True"
                          Background="White"
                          BorderBrush="#DEE2E6"
                          BorderThickness="1"
                          RowHeight="38"
                          ColumnWidth="*"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          AlternatingRowBackground="#F8F9FA"
                          HorizontalScrollBarVisibility="Disabled">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Sicil No"
                                            Binding="{Binding PersonnelCode}"
                                            Width="1.5*"
                                            FontSize="14"/>
                        <DataGridTextColumn Header="Ad Soyad"
                                            Binding="{Binding Name}"
                                            Width="3*"
                                            FontSize="14"/>
                        <DataGridTextColumn Header="TC Kimlik No"
                                            Binding="{Binding TCNo}"
                                            Width="2*"
                                            FontSize="14"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Butonlar -->
        <Grid Grid.Row="2" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       x:Name="txtRecordCount"
                       Text="Toplam 0 kayıt bulundu"
                       FontSize="14"
                       FontFamily="pack://application:,,,/Fonts/Poppins-Regular.ttf#Poppins"
                       Foreground="#6C757D"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    x:Name="btnConfirm"
                    Style="{StaticResource ModalButton}"
                    Width="160"
                    Height="42"
                    Click="btnConfirm_Click">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="&#xE73E;"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="16"
                               Margin="0,0,8,0"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="Tamam"
                               FontFamily="pack://application:,,,/Fonts/Poppins-Medium.ttf#Poppins"
                               FontSize="14"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Window>
